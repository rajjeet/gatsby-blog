import React from 'react';
import Layout from '../components/Layout';

export default () => (
  <Layout>
    <h4>Error 404:</h4>
    <p> This page is not found. Please enter a valid URL path into the address bar.</p>
    <a href="/">Go to Home</a>
  </Layout>
);
